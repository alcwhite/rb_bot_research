# frozen_string_literal: true

RSpec.describe RbBotResearch do
  it "has a version number" do
    expect(RbBotResearch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
